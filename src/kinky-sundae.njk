---
layout: layouts/kinky.njk
permalink: /kinky-sundae/
published: false
title: "Kinky Sundae · Module · Copenhagen"
description: "Kinky Sundae — techno, ink and kink built on consent. One Sunday a month at Module. Sex-positive, radically LGBTQ+-friendly, 20+. Sign up with Orbit."
ogImage: "/_assets/img/kinky-sundae-og.png"
---
{#- Landing page for Kinky Sundae (Ink & Art × Module). Build spec: BYG-240.
    Main language English (≈50% international crowd); Danish is a follow-up.
    Copy is LOCKED (awaiting HP's legal review). Guardrails:
    - do NOT promise live ink (the night = flash/booking; ink is set at the studio)
    - safety = effort, not a guarantee
    - no sex-for-payment framing, no anonymity promise
    House rules bake in Module's official Safer Space guidelines.
    Placeholders for Steven: Orbit signup URL (#tilmeld button), Shopify payment product. -#}

<div class="top">
  <div class="marq" aria-hidden="true">
    <div class="marq__t">CONSENT IS EVERYTHING <span>·</span> TECHNO <span>·</span> INK <span>·</span> KINK <span>·</span> LGBTQ+ <span>·</span> 20+ <span>·</span> KINKY SUNDAE X MODULE <span>·</span> SUN 2 AUG · 10 → 05 <span>·</span> CONSENT IS EVERYTHING <span>·</span> TECHNO <span>·</span> INK <span>·</span> KINK <span>·</span> LGBTQ+ <span>·</span> 20+ <span>·</span> KINKY SUNDAE X MODULE <span>·</span> SUN 2 AUG · 10 → 05 <span>·</span></div>
  </div>
  <div class="wrap topbar">
    <div class="topbar__mark">KINKY <b>SUNDAE</b></div>
    <a class="topbar__cta" href="#signup">Sign up</a>
  </div>
</div>

<header class="hero">
  <div class="wrap">
    <p class="eyebrow hero__eyebrow">{{ kinky.hero.eyebrow | safe }}</p>
    <h1 class="hero__wm">Kinky<br>Sun<span class="red">dae</span></h1>
    <div class="hero__meta">
      <div>{{ kinky.hero.metaLines[0] | safe }}</div>
      <div>{{ kinky.hero.metaLines[1] | safe }}</div>
      <div>{{ kinky.hero.metaLines[2] | safe }}</div>
    </div>
    <p class="hero__thesis">{{ kinky.hero.thesis | safe }}</p>
  </div>
</header>

<section class="manifest" aria-label="Manifesto">
  <div class="wrap">
    <p>{{ kinky.manifest.lines[0] | safe }}</p>
    <p>{{ kinky.manifest.lines[1] | safe }}</p>
    <p>{{ kinky.manifest.lines[2] | safe }}</p>
    <p>{{ kinky.manifest.lines[3] | safe }}</p>
    <p class="manifest__note">{{ kinky.manifest.note | safe }}</p>
  </div>
</section>

<section aria-labelledby="isityou">
  <div class="wrap">
    <div class="sec-label"><span class="n">01</span><h2 id="isityou">{{ kinky.isItYou.heading | safe }}</h2></div>
    <div class="split">
      <div class="split__col split__for">
        <h3 class="split__h">{{ kinky.isItYou.forTitle | safe }}</h3>
        <ul>
          <li>{{ kinky.isItYou.forItems[0] | safe }}</li>
          <li>{{ kinky.isItYou.forItems[1] | safe }}</li>
          <li>{{ kinky.isItYou.forItems[2] | safe }}</li>
          <li>{{ kinky.isItYou.forItems[3] | safe }}</li>
          <li>{{ kinky.isItYou.forItems[4] | safe }}</li>
        </ul>
      </div>
      <div class="split__col split__no">
        <h3 class="split__h">{{ kinky.isItYou.noTitle | safe }}</h3>
        <ul>
          <li>{{ kinky.isItYou.noItems[0] | safe }}</li>
          <li>{{ kinky.isItYou.noItems[1] | safe }}</li>
          <li>{{ kinky.isItYou.noItems[2] | safe }}</li>
          <li>{{ kinky.isItYou.noItems[3] | safe }}</li>
          <li>{{ kinky.isItYou.noItems[4] | safe }}</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<section class="rules" id="rules" aria-labelledby="rules-h">
  <div class="wrap">
    <div class="sec-label"><span class="n">02</span><h2 id="rules-h">{{ kinky.rules.heading | safe }}</h2></div>
    <p class="rules__intro">{{ kinky.rules.intro | safe }}</p>
    <div class="rules__grid">
      <div class="rule rule--wide">
        <div class="rule__n">{{ kinky.rules.items[0].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[0].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[0].d | safe }}</div>
      </div>
      <div class="rule">
        <div class="rule__n">{{ kinky.rules.items[1].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[1].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[1].d | safe }}</div>
      </div>
      <div class="rule">
        <div class="rule__n">{{ kinky.rules.items[2].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[2].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[2].d | safe }}</div>
      </div>
      <div class="rule">
        <div class="rule__n">{{ kinky.rules.items[3].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[3].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[3].d | safe }}</div>
      </div>
      <div class="rule">
        <div class="rule__n">{{ kinky.rules.items[4].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[4].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[4].d | safe }}</div>
      </div>
      <div class="rule">
        <div class="rule__n">{{ kinky.rules.items[5].n | safe }}</div>
        <div class="rule__t">{{ kinky.rules.items[5].t | safe }}</div>
        <div class="rule__d">{{ kinky.rules.items[5].d | safe }}</div>
      </div>
    </div>
  </div>
</section>

<section id="thenight" aria-labelledby="thenight-h">
  <div class="wrap">
    <div class="sec-label"><span class="n">03</span><h2 id="thenight-h">{{ kinky.night.heading | safe }}</h2></div>
    <div class="zones">
      <div class="zone">
        <div class="zone__k">{{ kinky.night.zones[0].k | safe }}</div>
        <div class="zone__t">{{ kinky.night.zones[0].t | safe }}</div>
        <p class="zone__d">{{ kinky.night.zones[0].d | safe }}</p>
      </div>
      <div class="zone">
        <div class="zone__k">{{ kinky.night.zones[1].k | safe }}</div>
        <div class="zone__t">{{ kinky.night.zones[1].t | safe }}</div>
        <p class="zone__d">{{ kinky.night.zones[1].d | safe }}</p>
      </div>
      <div class="zone">
        <div class="zone__k">{{ kinky.night.zones[2].k | safe }}</div>
        <div class="zone__t">{{ kinky.night.zones[2].t | safe }}</div>
        <p class="zone__d">{{ kinky.night.zones[2].d | safe }}</p>
      </div>
    </div>
  </div>
</section>

<section class="ink-strip" aria-labelledby="ink-h">
  <div class="wrap ink-strip__grid">
    <div>
      <p class="eyebrow">{{ kinky.ink.eyebrow | safe }}</p>
      <p class="ink-strip__big" id="ink-h">{{ kinky.ink.big | safe }}</p>
    </div>
    <div class="ink-strip__body">
      <p>{{ kinky.ink.body[0] | safe }}</p>
      <p>{{ kinky.ink.body[1] | safe }}</p>
    </div>
  </div>
</section>

<section id="door" aria-labelledby="door-h">
  <div class="wrap">
    <div class="sec-label"><span class="n">04</span><h2 id="door-h">{{ kinky.door.heading | safe }}</h2></div>
    <div class="facts">
      <div class="fact">
        <div class="fact__k">{{ kinky.door.facts[0].k | safe }}</div>
        <div class="fact__v">{{ kinky.door.facts[0].v | safe }}<small>{{ kinky.door.facts[0].small | safe }}</small></div>
      </div>
      <div class="fact">
        <div class="fact__k">{{ kinky.door.facts[1].k | safe }}</div>
        <div class="fact__v">{{ kinky.door.facts[1].v | safe }}<small>{{ kinky.door.facts[1].small | safe }}</small></div>
      </div>
      <div class="fact">
        <div class="fact__k">{{ kinky.door.facts[2].k | safe }}</div>
        <div class="fact__v">{{ kinky.door.facts[2].v | safe }}<small>{{ kinky.door.facts[2].small | safe }}</small></div>
      </div>
      <div class="fact">
        <div class="fact__k">{{ kinky.door.facts[3].k | safe }}</div>
        <div class="fact__v">{{ kinky.door.facts[3].v | safe }}<small>{{ kinky.door.facts[3].small | safe }}</small></div>
      </div>
    </div>
    <p class="door-note">{{ kinky.door.note | safe }}</p>
  </div>
</section>

<section class="cta" id="signup" aria-labelledby="signup-h">
  <div class="wrap">
    <p class="eyebrow">{{ kinky.cta.eyebrow | safe }}</p>
    <h2 class="cta__h" id="signup-h">{{ kinky.cta.h | safe }}</h2>
    <p class="cta__sub">{{ kinky.cta.sub | safe }}</p>
    <div class="price">
      <div class="price__c price__c--orbit">
        <div class="price__tag">{{ kinky.cta.prices[0].tag | safe }}</div>
        <div class="price__amt">{{ kinky.cta.prices[0].amt | safe }}</div>
      </div>
      <div class="price__c">
        <div class="price__tag">{{ kinky.cta.prices[1].tag | safe }}</div>
        <div class="price__amt">{{ kinky.cta.prices[1].amt | safe }}</div>
      </div>
    </div>
    <form class="cta__form" data-subscribe data-source="kinky-sundae" data-lang="en" data-reveal="#orbit-done" novalidate>
      <label class="cta__field">
        <span class="visually-hidden">Email</span>
        <input type="email" name="email" inputmode="email" autocomplete="email" placeholder="{{ kinky.cta.placeholder }}" required aria-required="true" />
      </label>
      <div class="cta__hp" aria-hidden="true"><label>Company<input type="text" name="company" tabindex="-1" autocomplete="off" /></label></div>
      <button type="submit" class="cta__btn">{{ kinky.cta.button | safe }}</button>
      <p class="cta__status" data-nl-status role="status" aria-live="polite"></p>
    </form>
    <div class="cta__done" id="orbit-done" hidden>
      <p>{{ kinky.cta.done | safe }}</p>
      <a class="cta__btn cta__btn--ticket" href="https://d1qp54-0w.myshopify.com/cart/53477074731336:1?attributes%5BEvent%5D=Kinky%20Sundae%202%20Aug">{{ kinky.cta.ticketLabel | safe }}</a>
    </div>
    <noscript><p class="cta__status">Enable JavaScript to sign up, or DM us on <a href="{{ site.instagram }}" target="_blank" rel="noopener noreferrer">Instagram</a>.</p></noscript>
    <p class="cta__fine">{{ kinky.cta.fine | safe }}</p>
  </div>
</section>

<footer class="ks-foot">
  <div class="wrap ks-foot__row">
    <div class="ks-foot__mark">KINKY <b>SUNDAE</b> X MODULE</div>
    <div class="ks-foot__meta">{{ kinky.foot.meta | safe }}</div>
  </div>
</footer>
