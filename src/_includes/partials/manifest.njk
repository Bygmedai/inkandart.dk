{% set lang = lang or (page.url | pageLang) %}
<section class="manifest">
  <p class="manifest__q">{% if lang == "en" %}Classic craft. Sharp attitude. Open late.<span class="p">The door is always open for someone.</span><span class="p manifest__note">Piercing — more than one? We do them the same day.</span>{% else %}Klassisk håndværk. Fed attitude. Sent åbent.<span class="p">Døren går altid op for nogen.</span><span class="p manifest__note">Piercing — mere end ét? Vi sætter dem samme dag.</span>{% endif %}</p>
</section>
